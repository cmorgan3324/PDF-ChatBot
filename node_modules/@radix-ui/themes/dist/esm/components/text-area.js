import * as React from 'react';
import classNames from 'classnames';
import { textAreaPropDefs } from './text-area.props';
import { extractMarginProps, withMarginProps, withBreakpoints } from '../helpers';
const TextArea = React.forwardRef((props, forwardedRef) => {
    const { rest: marginRest, ...marginProps } = extractMarginProps(props);
    const { className, size = textAreaPropDefs.size.default, variant = textAreaPropDefs.variant.default, color = textAreaPropDefs.color.default, style, ...textAreaProps } = marginRest;
    return (React.createElement("div", { style: style, className: classNames('rt-TextAreaRoot', className, withBreakpoints(size, 'rt-r-size'), `rt-variant-${variant}`, withMarginProps(marginProps)) },
        React.createElement("textarea", { "data-accent-color": color, className: "rt-TextAreaInput", ref: forwardedRef, ...textAreaProps }),
        React.createElement("div", { "data-accent-color": color, className: "rt-TextAreaChrome" })));
});
TextArea.displayName = 'TextArea';
export { TextArea };
//# sourceMappingURL=text-area.js.map